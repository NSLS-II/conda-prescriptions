package:
  name: historydict
  version: {{ environ.get('GIT_DESCRIBE_TAG', 'v0')[1:] }}.post{{ environ.get('GIT_DESCRIBE_NUMBER', ) }}

source:
  git_url: https://github.com/Nikea/historydict.git
  git_rev: master

build:
  number: 0

  # Note that this will override the default build string with the Python
  # and NumPy versions


requirements:
  build:
    - python

  run:
    - python
    - six

test:
  imports:
    - historydict
